import React from "react";

function NotFoundPage() {
    const NOT_FOUND_TEXT = "Sorry, the page is not available at the moment.";

    return (
          <h1><strong>{NOT_FOUND_TEXT}</strong></h1>
    );
}

export default NotFoundPage;